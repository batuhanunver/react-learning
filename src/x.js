


import React, { Component } from 'react';
import { View, Text, Platform, Button, Alert } from 'react-native';


export class X extends Component {
    render() {
        return (
            <View>
                <Text>

                </Text>
            </View>
        )
    }
}
this.first.group.questions.map((v) => (
    <Group key={v} title={} onYesSelected={() => { this.questionYesSelectd(v) }} onNoSelected={} />
))
